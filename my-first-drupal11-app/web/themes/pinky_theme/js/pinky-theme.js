/**
 * @file
 * pinky_theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.pinkyTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
